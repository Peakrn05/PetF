# Base URL of the PetB backend API.
# REQUIRED for production — without it the app falls back to http://localhost:4000/api
# and silently shows mock data instead of real bookings.
# Copy this file to .env.local and set the real value before launching.
NEXT_PUBLIC_API_URL=https://your-backend-domain.com/api
